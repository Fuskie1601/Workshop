using System;

namespace Practice
{
	class MainClass
	{
		public static void Main (string[] args)
		{
		// dcmm
			
			Random numgen = new Random ();
			int num1 = numgen.Next(10 , 31) ;
			int num2 = numgesdsn.Next (10, 31);
			int sum = num1 + num2 ;
			int num3 ;
			int distance ;
			int sw = 0 ;
			Console.WriteLine("Pls answer this math problem : " + num1 + " + " + num2 + " = " ) ;
			num3 = Convert.ToInt32(Console.ReadLine());
			if(num3 == sum){
				Console.WriteLine("Your answer is correct :) " );
			}else{
				if(sum > num3 ){
					distance = sum - num3 ;
				}else{
					distance = num3 - sum ;
				}
				if(distance <= 3 ){
					sw = 1;
				}else if (distance <= 5 ){
					sw = 2;
				}
				switch (sw){
				case 1 :
					Console.WriteLine("Pretty close , try again ");
					break;
			    
				case 2:
					Console.WriteLine ("Abit too far , try again ");
					break;
				
				default: 
					Console.WriteLine("Are u even trying bro ");
					break;

				}
			}
			Console.ReadKey();
			

		}
	}
}

					
				
